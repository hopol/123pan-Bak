[project]
name = "123pan"
version = "4.0.4"
description = "第三方123云盘客户端，解决了123云盘电脑客户端限制每日1GB自用流量的问题 "
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pyside6>=6.10",
    "pyside6-fluent-widgets>=1.11.1",
    "requests>=2.32.5",
    "zstandard>=0.25.0",
    "cryptography>=50.0.0",
    "shiboken6>=6.10",
    "qrcode[pil]>=8.2",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]

[tool.coverage.run]
source = ["src"]

[tool.coverage.report]
exclude_lines = ["pragma: no cover", "if TYPE_CHECKING:"]

[dependency-groups]
build = ["nuitka>=4.1.3", "maturin>=1.14.0", "imageio>=2.37.0"]
lint = ["pylint>=4.0.5", "mypy>=1.19.1"]
dev = ["pyrefly>=0.60.0"]
format = ["black>=26.5.1"]
test = [
    "pytest>=8.0.0",
    "pytest-mock>=3.14.0",
    "responses>=0.25.0",
    "coverage>=7.6.0",
]

[[tool.uv.index]]
url = "https://mirrors.aliyun.com/pypi/simple/"
